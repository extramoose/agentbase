name: Apply DB Migrations

on:
  push:
    branches:
      - main
    paths:
      - 'supabase/migrations/**.sql'

  # Allow manual trigger from GitHub Actions UI
  workflow_dispatch:

jobs:
  migrate:
    name: Apply pending migrations
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run migrations
        env:
          PGHOST: db.lecsqzctdfjwencberdj.supabase.co
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
          PGDATABASE: postgres
          PGSSLMODE: require
        run: |
          # Resolve to IPv4 — GitHub Actions runners have no IPv6
          export PGHOSTADDR=$(getent ahostsv4 $PGHOST | head -1 | awk '{print $1}')
          echo "Connecting via $PGHOSTADDR"
          python3 scripts/migrate.py
